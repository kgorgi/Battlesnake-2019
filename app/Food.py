class Food:

    def __init__(self, data, snake):
        self.x = data['x']
        self.y = data['y']

    def dist(self.)
        self.distance = abs(snake.x - self.x) + abs(snake.y - self.y)
        

